{% extends 'base.html.twig' %}

{% block title %}inscription
{% endblock %}
{% block body %}

	<div class="my-auto mx-auto w-100  ">
		<div class=" mx-auto   col-10 offset-lg-3 col-lg-4  position-relative">
			<div class=" mb-3 text-center rounded">
				<h1>Inscription</h1>
				<div class="rectangle bg-primary-inverse col-7 col-lg-9 col-xl-7 mx-auto p-5 rounded ">
					{{ form_errors(registrationForm) }}
					{{ form_start(registrationForm) }}
					{{ form_row(registrationForm.email, {
                                    label: 'E-mail :'
                                }) }}
					{{ form_row(registrationForm.firstname, {
                                    label: "Prénom :"
                                }) }}
					{{ form_row(registrationForm.lastname, {
                                    label: "Nom :"
                                }) }}
					{{ form_row(registrationForm.plainPassword, {
                                    label: 'Mot de passe :'
                                }) }}
					<div class="d-flex justify-content-center m-3">
						<button type="submit" class="btn btn-primary ">S'inscrire</button>
					</div>
					{{ form_end(registrationForm) }}
				</div>
			</div>
		</div>
	</div>
</div>{% endblock %}
